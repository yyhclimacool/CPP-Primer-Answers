#include <iostream>

using namespace std;

int main () {
    int *pi, i, &r = i; // pi is a pointer, i is an int and r is a ref of int
    int i1, *ip1 = 0; // i is an int, ip is a pointer to an int
    int *ip2, ip3; // ip is a pointer to an int, ips is an int
}
